<div class="col-md-4 col-sm-6 col-6 user-card-container mb-5">
  <%= link_to user_path(user), class: "card user-card" do %>
    <%= image_tag(display_picture_for(user), class: "card-img-top w-100") %>
    <div class="card-body">
      <p class="h5 card-title"><%= user.username %></p>
      <p class="card-text"><%= user.age %> • <%= user.religion.humanize %></p>
      <div class="country-name">
        <%= user.country_name %>
      </div>
    </div>
  <% end %>
</div>
