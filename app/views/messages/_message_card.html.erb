<%=link_to conversation_path(message.conversation_id), { class: 'message-card' } do %>
  <div class="media p-a-1">
    <div class="media-left">
      <%= image_tag(user.display_picture_for(current_user, :small_thumb), { class: 'img-circle', width: 80 })%>
    </div>
    <div class="media-body">
      <p class="media-heading username m-b-0 lead"><%= user.username %></p>
      <span class="message-body text-muted">
        <%= truncate message.body %>
      </span><br>
      <small class="text-muted"><%= time_ago_in_words(message.created_at) %> ago</small>
      <% unless message.read? %>
      <span class="tag tag-info">unread</span>
      <% end %>
    </div>
  </div>
<% end %>
