<%= form_for([@conversation.other_participant(current_user), Message.new], remote: true ) do |f| %>
  <div class="form-group">
    <%= f.text_area :body, { rows: 2, maxlength: 1000, class: 'form-control', placeholder: 'Say something', required: true, autofocus: true } %>
  </div>
  <div class="form-group text-right">
    <button type="submit" class="btn btn-secondary" data-disable-with="Sending…" >Reply</button>
  </div>
<% end %>
